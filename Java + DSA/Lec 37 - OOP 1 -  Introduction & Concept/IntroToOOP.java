public class IntroToOOP{
    public static void main(String[] args) {
        // You are told to create a data type to store 5 names
        String[] names = new String[5];

        // Now, Create something to store 5 roll no.
        int[] roll = new int[5];
        System.out.println(names);
        System.out.println(roll);


        // Create a Data Type to store data of 5 students {name, rollNo, marks}
        
        // It's probably not possible to created a single variable (obj) to store all of this.
        // This way OOP is used. We can create classes to store data(variables) + operations(methods).

        // Let's create a custom class & object in next file.
    }
}